# Hdy08/motrix-next:main 分支工作约束

本文件约束在本仓库内进行的后续工作。目标是让每次可交付的源码变更都能可靠回溯，并保留可运行的安装包。本文件的优先级高于其他约束文件，如AGENTS.md和README.md。

## 提交，推送，发布与变更日志

1. 每完成一次源码改动，必须在本地创建 Git 提交。提交只能包含本次任务所需的安全、必要且完整的改动；不得夹带无关格式化、生成物或他人的未提交修改。只能提交到 Hdy08/motrix-next:main 分支。
2. 提交前检查 `git status --short` 与暂存区内容；工作区原有的无关修改必须保留，不得覆盖、回退或一并提交。
3. 根目录的 `CHANGELOG.md` 必须使用中文记录每次源码改动。每个条目至少包含：日期、对应 Git 提交短哈希与主题、改动内容、影响范围、验证结果，以及安装包。
4. 为保证 Changelog 中的短哈希真实准确，先提交源码改动，再立即以独立文档提交写入该源码提交的短哈希和 Changelog 条目；不得修改、猜测或尝试自引用源码提交哈希。
5. 文档或 Changelog 的独立修改也应提交，但不需要因为纯文档修改而触发完整测试、生成或打包。
6. 用户有明确推送指示时，只能推送到 Hdy08/motrix-next:main 的远端仓库。
7. 用户有明确发布指示时，流程参见AGENTS.md，必须将修改远端地址替换为 Hdy08/motrix-next 的。

## 上游合并与功能保护

1. 用户要求尝试合并上游更新时，必须以当前分支已经提交的历史为保护基线。禁止修改、回退、重写或丢弃当前分支已提交的改动，也不得因合并而删除、替换、降级或改变当前分支新增功能的既有行为。
2. 合并前必须检查上游提交与当前分支的差异及祖先关系；仅当可以在完整保留当前分支已提交改动和新增功能的前提下，才可继续合并。
3. 如存在无法避免的冲突，或上游已实现相同、相近或可能重叠的功能，必须在处理冲突或选择实现方案前向用户说明可选方案、各自影响和建议，等待用户明确决定。不得自行选择保留上游或本地实现。

## 测试，打包与清理

1. 测试，打包流程参见其他约束文件，如AGENTS.md和README.md。
2. 源码改动完成后，代理不主动执行打包或清理。打包和临时产物清理由用户手动运行根目录下的单文件脚本（待补充，要求可以直接运行）完成。除非用户另有明确指示，代理不得主动启动打包脚本或删除中间产物；
3. 安装包生成路径在 `.\dist` 。禁止覆盖、替换、重命名或删除既有安装包。新安装包必须携带时间和对应提交短哈希后缀，格式 `yyyyMMdd-HHmmss_短哈希` ，例如：
   ```text
   dist\MotrixNext_3.9.7-beta.2_x64-setup_20260728-140844_f0875ea.exe
   ```
4. 需要清理的文件包括编译产物，中间产物等**临时文件**，禁止误触需要提交的源码等。
5. 常规打包必须传入包含时间和当前提交短哈希的唯一后缀；未满足这一条件时不得执行打包。
6. 安装包完成后必须验证 SHA-256，并在 `CHANGELOG.md` 中写入文件名、哈希。
